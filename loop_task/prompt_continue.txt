The scenario is "{scenario}".
One of the steps towards that is:{steps}.
Step {loop_idx} "{loop_step}" involves a loop.

You need to generate three state descriptions that each independently would lead to the conclusion that the loop should **continue**.
- The first description should be an easy, directly observable state (e.g., the target condition has not yet been met).
- The second description should be a medium, less immediately visible state that still clearly implies the loop should continue (e.g., an intermediate sign that progress is being made but the end is not reached).
- The third description should be a hard, even less directly observable state that is still a sufficient reason to conclude the loop should continue (e.g., a subtle or indirect indicator that the loop's condition is not satisfied).

Requirements:
- Each description must be a standalone, complete, sufficient reason to conclude the loop should continue. No single reason should require reading the others to make sense.
- Each description must describe a state, not an argument for why the loop should continue. Use concrete, factual sentences.
- Keep descriptions concise, typically one sentence.

Return a JSON array of three strings, corresponding to easy, medium, hard difficulty in that order.
Example output: ["the water in the pot is not boiling yet, only small bubbles at the bottom", "the steam from the kettle is still weak and intermittent", "the automatic shut-off sensor has not been triggered and the heating element is still drawing full power"]

Only output the JSON array, nothing else.