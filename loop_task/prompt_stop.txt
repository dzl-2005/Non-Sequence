The scenario is "{scenario}".
One of the steps towards that is:{steps}.
Step {loop_idx} "{loop_step}" involves a loop.

You need to generate three state descriptions that each independently would lead to the conclusion that the loop should **stop**.
- The first description should be an easy, directly observable state (e.g., the target condition has been met).
- The second description should be a medium, less immediately visible state that still clearly implies the loop should stop (e.g., a sign that the goal is achieved but not instantly obvious).
- The third description should be a hard, even less directly observable state that is still a sufficient reason to conclude the loop should stop (e.g., a subtle indicator or indirect evidence that the loop's goal has been reached).

Requirements:
- Each description must be a standalone, complete, sufficient reason to conclude the loop should stop. No single reason should require reading the others to make sense.
- Each description must describe a state, not an argument for why the loop should stop. Use concrete, factual sentences.
- Keep descriptions concise, typically one sentence.

Return a JSON array of three strings, corresponding to easy, medium, hard difficulty in that order.
Example output: ["the lock screen shows 100% battery", "the charging indicator light has turned from red to green", "the power management IC reports charge current has dropped to trickle level, indicating a full battery"]

Only output the JSON array, nothing else.